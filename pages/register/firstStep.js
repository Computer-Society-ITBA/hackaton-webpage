import { Step, Steps, useSteps } from 'chakra-ui-steps';
import {
  Heading,
  Flex,
  Box,
  Text,
  useColorModeValue,
  useBreakpointValue,
  Button,
  Link,
  Grid,
} from "@chakra-ui/react";

const FirstStep = () => {
  return (
    <h1>PRIMER PASO</h1> //Aca armo el primer step del form
  );
};

export default FirstStep